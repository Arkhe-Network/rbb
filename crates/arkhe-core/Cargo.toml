[package]
name = "arkhe-core"
version = "2.2.0"
edition = "2021"

[dependencies]
# Fix R3: dependência `uuid` removida.
chrono = { version = "0.4", features = ["serde", "clock"] }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
pretty_assertions = "1.4.1"
regex = "1.0"
